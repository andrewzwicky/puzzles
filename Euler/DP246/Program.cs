using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace DP246
{
   class Program
   {
      static void Main(string[] args)
      {
      }

      static List<int[]> Possibles(string numbers)
      {
         char first = numbers.First();
         string rest = numbers.Skip(1).ToString();


      }
   }
}
